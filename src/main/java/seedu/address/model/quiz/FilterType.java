package seedu.address.model.quiz;

/**
 * This documents the types of filter to filter the quiz results by.
 */
public enum FilterType {
    NONE,
    SUBJECT,
    DIFFICULTY,
    CORRECT,
    INCORRECT,
    DATE
}
