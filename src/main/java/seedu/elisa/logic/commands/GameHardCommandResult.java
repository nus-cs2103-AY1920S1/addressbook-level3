package seedu.elisa.logic.commands;

/**
 * Command Result class for hard mode of game.
 */
public class GameHardCommandResult extends CommandResult {
    public GameHardCommandResult(String feedbackToUser) {
        super(feedbackToUser);
    }
}
