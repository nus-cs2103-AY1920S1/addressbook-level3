package seedu.deliverymans.model.deliveryman.deliverymanstatistics;

/**
 * Represents the level of data variables in {@code StatisticsRecordCard}.
 */
public enum State {
    LOW, MODERATE, HIGH, MAX;
}
