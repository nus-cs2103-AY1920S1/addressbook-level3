package seedu.sugarmummy.model;

/**
 * Unmodifiable view of an arbitrary data.
 */
public interface ReadOnlyData {
    /**
     * Implements methods to return an unmodifiable view of data containing models represented by field objects
     * in the program.
     */

    /**
     * String representation of read-only data.
     */
    String toString();
}
