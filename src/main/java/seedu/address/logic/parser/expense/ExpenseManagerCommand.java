package seedu.address.logic.parser.expense;

/**
 * Enumeration of the commands related to the expense manager page.
 */
public enum ExpenseManagerCommand {
    CREATE,
    GOTO,
    EDIT,
    DELETE,
    HOME,
    DAYS,
    ITINERARY
}
