package cs.f10.t1.nursetraverse.autocomplete;

import org.junit.jupiter.api.Test;

class AutoCompleteListHandlerTest {

    /*private AutoCompleteWordStorage autoCompleteWordStorage =
    new AutoCompleteWordStorage(new FilteredList<Patient>());
    private AutoCompleteListHandler autoCompleteListHandler = new AutoCompleteListHandler(autoCompleteWordStorage);

    @Test
    void chooseInitialList() {
        LinkedList<AutoCompleteWord> dummyLL = new LinkedList<>();
        ObservableList<AutoCompleteWord> chosenList = autoCompleteListHandler.chooseInitialList(dummyLL);
        assertTrue(chosenList.get(0) instanceof ObjectWord);
    }*/

    @Test
    void updateList() {
    }

    @Test
    void filterList() {
    }
}
