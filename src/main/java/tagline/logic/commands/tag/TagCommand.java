package tagline.logic.commands.tag;

import tagline.logic.commands.Command;

/**
 * Represents a contact command with hidden internal logic and ability to be executed.
 */
public abstract class TagCommand extends Command {
    public static final String COMMAND_KEY = "tag";
}
