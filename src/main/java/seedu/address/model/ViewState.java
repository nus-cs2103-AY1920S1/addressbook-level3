package seedu.address.model;

/**
 * ViewState Enumerates the 3 different states for views available to be rendered in the UI.
 */
public enum ViewState {
    DEFAULT_EXPENSELIST,
    EXPENSELIST_IN_BUDGET,
    BUDGETLIST;
}
