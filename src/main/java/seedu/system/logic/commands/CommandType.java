package seedu.system.logic.commands;

/**
 * Represents different categories of commands. Intended for varying the display in v2.0.
 * Guarantees: immutable
 */
public enum CommandType {
    COMPETITION,
    PARTICIPATION,
    PERSON,
    GENERAL
}
