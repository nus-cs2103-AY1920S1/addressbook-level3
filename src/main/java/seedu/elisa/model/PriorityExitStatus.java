package seedu.elisa.model;

/**
 * Enumeration containing the different ways in which one can exit the priority mode.
 * Helps to personalize the feed back messages to the users.
 */
public enum PriorityExitStatus {
    ALL_TASK_COMPLETED, PRIORITY_TIMEOUT
}
