package seedu.address.logic.parser.inventory;

/**
 * Enum of possible commands that the user can type in the InventoryView
 */
public enum InventoryViewCommand {
    ADD,
    DELETE,
    CHECK,
    UNCHECK
}
