package seedu.address.autocomplete;

/**
 * Represents a key word(eg: patient ic, patient name) in application
 */
public class KeyWord extends AutoCompleteWord {
    public KeyWord(String suggestionWord) {
        super(suggestionWord);
    }
}
