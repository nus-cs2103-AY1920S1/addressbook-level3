package seedu.system.logic.commands;

/**
 * Represents different categories of commands.
 * Guarantees: immutable
 */
public enum CommandType {
    COMPETITION,
    PARTICIPATION,
    PERSON,
    GENERAL
}
