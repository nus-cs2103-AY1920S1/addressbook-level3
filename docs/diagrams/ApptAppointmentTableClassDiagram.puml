@startuml
!include style.puml
skinparam arrowThickness 1.1
skinparam arrowColor UI_COLOR_T4
skinparam classBackgroundColor UI_COLOR

show members

  namespace seedu.address {
    namespace model {
      namespace appointment {
        class seedu.address.model.appointment.AppointmentTable {
            - followup : HashMap<String, Integer>
            - reminders : HashMap<String, Integer>
            + AppointmentTable()
            + addAppointment()
            + cascadeDay()
            + deleteAppointment()
            + equals()
            + getAppointmentList()
            {static} + getDefaultAppointments()
            + hashCode()
            + outputAppointments()
            + sortAppointments()
            + toString()
            - antiDuplicate()
            - sort()
        }
      }
    }
  }

@enduml
