package seedu.address.logic.commands.result;

/**
 * UiFocus tells the Ui which tabs to expand.
 */
public enum UiFocus {
    AGENDA,
    INFO,
    HELP,
    ACTIVITY,
    ACCOMMODATION,
    CONTACT
}
