package seedu.address.logic.commands;

public class UploadCommandTest {
    /*
    private Model model = new ModelManager(getTypicalNotebook(), new UserPrefs());

    @Test
    public void execute_uploadCommand_success() {
        showStudentAtIndex(model, INDEX_FIRST_OBJECT);

        Student studentInFilteredList = model.getFilteredStudentList().get(INDEX_FIRST_STUDENT.getZeroBased());
        Student editedStudent = new StudentBuilder(studentInFilteredList).withName(VALID_NAME_BOB).build();
        UploadPictureCommand uploadCommand = new UploadPictureCommand(INDEX_FIRST_STUDENT, FILE_EXAMPLE_1);

        String expectedMessage = String.format(UploadPictureCommand., editedStudent);

        Model expectedModel = new ModelManager(new Notebook(model.getNotebook()), new UserPrefs());
        expectedModel.setStudent(model.getFilteredStudentList().get(0), editedStudent);

        assertCommandSuccess(uploadCommand, model, expectedMessage, expectedModel);
    }
    @Test
    public void execute_invalidStudentIndexUnfilteredList_failure() {
        Index outOfBoundIndex = Index.fromOneBased(model.getFilteredStudentList().size() + 1);
        ResetDisplayPictureCommand editStudentCommand = new ResetDisplayPictureCommand(outOfBoundIndex);
        assertCommandFailure(editStudentCommand, model, Messages.MESSAGE_INVALID_STUDENT_DISPLAYED_INDEX);
    }
     */
}
