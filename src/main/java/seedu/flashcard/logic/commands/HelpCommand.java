package seedu.flashcard.logic.commands;

import seedu.flashcard.model.FlashcardList;

public class HelpCommand extends Command {

    public static final String COMMAND_WORD = "help";

    // TODO: Specify the message of usage of help command.
    public static final String MESSAGE_USAGE = "";

    // TODO: implement the following execution method for help command, beware to generate a good command result.
    // TODO: write corresponding tests to test out this execution methods.
    @Override
    public CommandResult execute(FlashcardList flashcardList) {
        return null;
    }
}
