package seedu.flashcard.logic.parser;

import seedu.flashcard.logic.commands.ViewCommand;

/**
 * Parses input arguments to generate {@Code ViewCommand}
 */
public class ViewCommandParser implements Parser {

    /**
     * TODO: Implement the following view command parser.
     * TODO: Write corresponding test cases for this parser.
     * Parses the string of arguments.
     * @param args string containing the parameters for the flashcard to view.
     * @return new add command.
     */
    @Override
    public ViewCommand parse(String args) {
        return null;
    }
}
