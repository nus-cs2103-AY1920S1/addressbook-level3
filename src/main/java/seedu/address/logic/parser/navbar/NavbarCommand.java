package seedu.address.logic.parser.navbar;

/**
 * Enumeration of navigation commands relating to pages that have the navbar.
 */
public enum NavbarCommand {
    HOME,
    DAYS,
    ITINERARY,
    DIARY
}
