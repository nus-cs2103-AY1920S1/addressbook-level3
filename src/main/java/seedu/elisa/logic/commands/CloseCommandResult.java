package seedu.elisa.logic.commands;

/**
 * Creates the command result of CloseCommand.
 */
public class CloseCommandResult extends CommandResult {
    public CloseCommandResult(String feedbackToUser) {
        super(feedbackToUser);
    }
}
