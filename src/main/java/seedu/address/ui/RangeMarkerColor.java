package seedu.address.ui;

import javafx.scene.paint.Color;

/**
 * Contains the color used for horizontal range markers in {@code CustomLineChart} common to {@code AverageGraph} and
 * {@code LegendPane}.
 */
public class RangeMarkerColor {
    public static final Color COLOR_BLUE = Color.BLUE.deriveColor(1, 1, 1, 0.25);
    public static final Color COLOR_GREEN = Color.GREEN.deriveColor(1, 1, 1, 0.25);
    public static final Color COLOR_YELLOW = Color.YELLOW.deriveColor(1, 1, 1, 0.25);
    public static final Color COLOR_RED = Color.RED.deriveColor(1, 1, 1, 0.25);
}
