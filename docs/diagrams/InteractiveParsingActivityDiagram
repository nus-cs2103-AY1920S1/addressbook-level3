@startuml

start

:read user input;

while (current state parameter's argument is present)
  :extract argument;
  if () then ([valid argument])
  :update state;
  else ([else])
  endif


  :remove processed argument;
endwhile

:notify user;

stop

@enduml