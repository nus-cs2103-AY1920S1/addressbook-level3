package seedu.flashcard.logic.parser;

import seedu.flashcard.logic.commands.AddCommand;

/**
 * Parses input arguments to generate {@Code AddCommand}
 */
public class AddCommandParser implements Parser<AddCommand> {

    /**
     * TODO: Implement the following add command parser.
     * TODO: Write corresponding test cases for this parser.
     * Parses the string of arguments to be added.
     * @param args string containing the parameters for the new model
     * @return new add command
     */
    public AddCommand parse (String args) {
        return null;
    }
}
