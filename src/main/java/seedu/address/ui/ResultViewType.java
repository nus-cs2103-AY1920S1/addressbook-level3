package seedu.address.ui;

/**
 * Represents the possible types of views that a command result requires.
 */
public enum ResultViewType {
    TEXT,
    TAG,
    MODULE,
    STUDY_PLAN,
    COMMIT_HISTORY
}
