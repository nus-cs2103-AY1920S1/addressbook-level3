package seedu.jarvis.model.financetracker;

/**
 * Determines the direction of which payment is owed. Payment is either owed to the user or by the user to someone else.
 */
public enum PaymentDirection {
    USEROWES,
    USERINDEBTTO
}
