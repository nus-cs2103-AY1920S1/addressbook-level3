package seedu.address.cashier.model;

public class SalesList {
/*
    //private static HashMap<Item, Integer> itemList;
    private static InventoryList itemList;

    public SalesList(InventoryList inventoryList) {
        //itemList = new HashMap<>();
        this.itemList = inventoryList;
    }

    //write into inventory file
    public String writeIntoFile() throws NoSuchIndexException {
        String msg = "";
        for (int i = 0; i < itemList.size(); i++) {
            Item item = itemList.getItem(i);
            msg += item.getDescription() + " | " + item.getCategory() + " | " + item.getQuantity() +
                    " | " + item.getCost();
        }
        return msg;
    }

    public InventoryList getItemList() {
        return this.itemList;
    }
*/
}

